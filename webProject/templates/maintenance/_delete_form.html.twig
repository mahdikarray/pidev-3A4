<form method="post" action="{{ path('app_maintenance_delete', {'idDemande': maintenance.idDemande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maintenance.idDemande) }}">
    <button class="btn">Delete</button>
</form>
